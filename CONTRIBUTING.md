# Руководство по участию в проекте

Спасибо за интерес к проекту Ecosystem Development! Это руководство поможет вам начать работу и внести свой вклад.

## Оглавление

- [Начало работы](#начало-работы)
- [Workflow участника](#workflow-участника)
- [Правила оформления документов](#правила-оформления-документов)
- [Система классификации](#система-классификации)
- [Работа с Obsidian](#работа-с-obsidian)
- [Автоматизация и скрипты](#автоматизация-и-скрипты)
- [Pull Request процесс](#pull-request-процесс)
- [Стандарты качества](#стандарты-качества)
- [Часто задаваемые вопросы](#часто-задаваемые-вопросы)

## Начало работы

### Предварительные требования

**Обязательно:**
- Git 2.30+
- Python 3.11+
- Текстовый редактор с поддержкой Markdown

**Рекомендуется:**
- [Obsidian](https://obsidian.md/) для удобной работы с документами
- WSL2 (для Windows) или Linux/macOS терминал
- GitHub CLI (`gh`) для работы с PR

### Первоначальная настройка

```bash
# 1. Fork репозитория через GitHub UI
# 2. Клонировать ваш fork
git clone https://github.com/YOUR_USERNAME/ecosystem-development.git
cd ecosystem-development

# 3. Добавить upstream remote
git remote add upstream https://github.com/ORIGINAL_ORG/ecosystem-development.git

# 4. Проверить Python зависимости
python3 --version  # должно быть 3.11+

# 5. Установить pre-commit hooks (опционально)
# pip install pre-commit
# pre-commit install

# 6. Синхронизироваться с upstream
git fetch upstream
git checkout main
git merge upstream/main
```

### Структура ветвления

Мы используем следующую стратегию ветвления:

**Защищенные ветки:**
- `main` - production-ready код, изменения только через PR
- `feat/ci-and-ops` - экспериментальная ветка для CI/CD

**Ветки участников:**
- `feature/<название>` - новые функции и системы
- `docs/<название>` - документация
- `fix/<название>` - исправления ошибок

**Ветки AI-агентов:**
- `ai/<agent>/<YYYY-MM-DD>` - автоматически создаваемые агентами
- Примеры: `ai/architect/2025-11-12`, `ai/docs/2025-11-13`

## Workflow участника

### Типичный цикл работы

```bash
# 1. Синхронизация с upstream
git checkout main
git fetch upstream
git merge upstream/main
git push origin main

# 2. Создание feature-ветки
git checkout -b feature/add-new-system

# 3. Внесение изменений
# Редактируйте файлы в content/ или другие директории

# 4. Запуск локальных проверок
python3 ops/classify_documents.py      # Классификация новых документов
python3 ops/validate_classifications.py  # Проверка валидности

# 5. Коммит изменений
git add .
git commit -m "feat: добавлена новая система X

Описание:
- Создан документ 4.13. Новая система
- Обновлен список подсистем (4.1)
- Добавлена классификация"

# 6. Push в ваш fork
git push origin feature/add-new-system

# 7. Создание Pull Request через GitHub UI или gh CLI
gh pr create --title "feat: добавлена новая система X" \
  --body "Описание PR..." \
  --base main
```

### Правила коммитов

Мы используем [Conventional Commits](https://www.conventionalcommits.org/):

**Типы коммитов:**
- `feat:` - новая функция или система
- `docs:` - изменения в документации
- `fix:` - исправление ошибок
- `refactor:` - рефакторинг без изменения функциональности
- `style:` - форматирование, опечатки
- `test:` - добавление тестов
- `chore:` - обновление зависимостей, конфигов

**Примеры хороших коммитов:**
```bash
feat: добавлен документ 4.13. Система аналитики
docs: обновлен README с инструкциями по установке
fix: исправлена валидация frontmatter в classify_documents.py
refactor: переименованы разделы в документе 0.6
```

**Примеры плохих коммитов:**
```bash
update files           # Что обновлено?
fix                    # Что исправлено?
WIP                    # Не коммитьте незавершенную работу
asdfasdf               # Непонятное сообщение
```

## Правила оформления документов

### Frontmatter метаданные

Каждый новый документ в `content/` **обязан** содержать frontmatter:

```yaml
---
type: doc                 # doc, data, code, model, policy, contract, metric, economy
audience: manual          # manual, mixed, machine
edit_mode: manual         # manual, mixed, machine
layer: methodology        # philosophy, methodology, ontology, program, conops, requirements, architecture, service, data, analytics, economy, content
scope: global-core        # global-core, local-edge
security: public          # public, internal, restricted
status: draft             # draft, in_progress, approved, published, archived
---
```

**Обязательные поля:**
- `type`, `audience`, `edit_mode`, `layer`, `scope`, `security` - из системы классификации
- `status` - текущий статус документа

**Опциональные поля:**
```yaml
id: SYS-013               # Уникальный ID (для трассировки)
version: 0.2              # Версия документа
owner: "Команда Backend"  # Владелец документа
review: ["Tech Lead", "Architect"]  # Ревьюеры
links:                    # Связи с другими документами
  problem: PRB-021
  requirements: [REQ-127]
  architecture: [ARC-STAT-07]
```

### Структура документа

```markdown
---
# Frontmatter здесь
---

# Название документа

Краткое описание (1-2 предложения): что описывает документ, зачем он нужен.

## Основной контент

### Подразделы...

## Связанные документы

- [[Ссылка на документ 1]]
- [[Ссылка на документ 2]]

## Метаинформация

Владелец: Имя
Последнее обновление: YYYY-MM-DD
```

### Именование файлов

**Правила:**
1. Используйте нумерацию для порядка: `4.12. Название.md`
2. Используйте пробелы, не дефисы: `Единое хранилище знаний.md`
3. Расширение всегда `.md`
4. Кириллица допустима и предпочтительна для контента
5. Избегайте специальных символов: `/ \ : * ? " < > |`

**Примеры:**
```
✅ content/4. Системы/4.12. Единое хранилище знаний (Memory Bank).md
✅ content/0. Управление/0.6. Структура этого хранилища.md
❌ content/4-системы/4.12-memory-bank.md
❌ content/systems/memory_bank.md
```

### Markdown стиль

**Заголовки:**
```markdown
# H1 - только один в документе (название)
## H2 - основные разделы
### H3 - подразделы
#### H4 - детали (используйте редко)
```

**Ссылки:**
```markdown
# Внутренние ссылки (Obsidian-style)
[[0.6. Структура этого хранилища]]
[[4.1. Список подсистем#Раздел]]

# Внешние ссылки
[Текст ссылки](https://example.com)

# Относительные пути (для GitHub)
[Документ](./content/folder/file.md)
```

**Списки:**
```markdown
# Нумерованные
1. Первый пункт
2. Второй пункт
   - Вложенный маркированный

# Маркированные
- Пункт
  - Вложенный
    - Еще более вложенный

# Чекбоксы
- [ ] Задача не выполнена
- [x] Задача выполнена
```

**Таблицы:**
```markdown
| Колонка 1 | Колонка 2 | Колонка 3 |
|-----------|-----------|-----------|
| Значение 1 | Значение 2 | Значение 3 |
| Строка 2 | Строка 2 | Строка 2 |
```

**Код:**
````markdown
```python
# Блок кода с подсветкой синтаксиса
def example():
    return "Hello"
```

Inline код: `variable_name`
````

## Система классификации

### Цветовая маркировка

Документ [0.6. Структура этого хранилища](content/0.%20Управление/0.6.%20Структура%20этого%20хранилища.md) содержит таблицу классификации всех документов с цветовыми маркерами:

- 🟡 **Желтый круг** - предложение AI (можно править вручную)
- 🟢 **Зеленый круг** - ручная правка человека (**ЗАЩИЩЕНО**: AI никогда не изменит)

### Как защитить свою правку от AI

**Проблема:** AI-скрипт `classify_documents.py` может перезаписать ваши изменения в таблице классификации.

**Решение:** Используйте скрипт `save_manual_edits.py`:

```bash
# 1. Вручную отредактируйте таблицу в документе 0.6 (Obsidian или редактор)
# 2. Сохраните ручные правки (они станут защищенными)
python3 ops/save_manual_edits.py

# 3. Теперь AI не будет изменять защищенные ячейки
python3 ops/classify_documents.py

# 4. Проверьте: защищенные ячейки будут помечены 🟢 (зеленый круг)
```

### Оси классификации

Каждый документ классифицируется по **6 осям**:

#### Ось A: Type (Вид артефакта)
- `doc` - текстовый документ, руководство
- `data` - наборы данных, события
- `code` - код агентов, сервисов
- `model` - модели, онтологии
- `policy` - правила, политики
- `contract` - API-контракты, соглашения
- `metric` - метрики, дашборды
- `economy` - токен-экономика, биллинг

#### Ось B: Audience & Edit Mode (Читаемость и изменение)
- `manual` - только человек
- `mixed` - человек + AI
- `machine` - только AI/сервисы

#### Ось C: Layer (Семантический слой)
- `philosophy` - манифесты, ценности, цели
- `methodology` - методы, процессы, роли
- `ontology` - понятия, термины, словарь
- `program` - программы развития, руководства
- `conops` - концепции операций, сценарии
- `requirements` - требования к системам
- `architecture` - архитектура (C4, ADR)
- `service` - описания подсистем, API
- `data` - схемы данных, события
- `analytics` - аналитика, отчеты
- `economy` - токеномика, биллинг
- `content` - контент, маркетинг

#### Ось D: Scope (Область)
- `global-core` - ядро экосистемы, единые правила
- `local-edge` - локализация, региональные особенности

#### Ось E: Security (Уровень доступа)
- `public` - открытый доступ
- `internal` - только для команды
- `restricted` - ограниченный доступ

#### Статус документа
- `draft` - черновик
- `in_progress` - в работе
- `approved` - утвержден
- `published` - опубликован
- `archived` - архивирован

Подробнее: [content/0. Управление/0.7. Классификация документов и теги.md](content/0.%20Управление/0.7.%20Классификация%20документов%20и%20теги.md)

## Работа с Obsidian

### Установка и настройка

1. **Скачать Obsidian:** https://obsidian.md/
2. **Открыть vault:**
   - File → Open vault → Open folder as vault
   - Выбрать `/mnt/c/ecosystem-development` (или путь к вашему клону)

3. **Установить рекомендуемые плагины:**
   - Settings → Community plugins → Turn on community plugins
   - Browse → Search and install:
     - **Obsidian Git** - автосинхронизация с GitHub
     - **Dataview** - динамические запросы к документам
     - **Shell commands** - запуск скриптов из Obsidian

### Настройка Obsidian Git

```
Settings → Obsidian Git:
- Vault backup interval: 30 (автосохранение каждые 30 минут)
- Auto pull interval: 10 (автопулл каждые 10 минут)
- Commit message: "vault backup: {{date}}"
- Pull updates on startup: ON
- Push on backup: ON
```

### Настройка Shell commands

Добавьте команды для запуска скриптов:

```
Settings → Shell commands → New shell command:
1. bash ops/build_check_document.sh (Hotkey: Ctrl+Shift+B)
2. python3 ops/classify_documents.py (Hotkey: Ctrl+Shift+C)
3. python3 ops/validate_classifications.py (Hotkey: Ctrl+Shift+V)
```

### Полезные горячие клавиши Obsidian

- `Ctrl+N` - новый документ
- `Ctrl+O` - быстрый переход между файлами
- `Ctrl+P` - командная палитра
- `Ctrl+G` - открыть граф связей
- `Ctrl+Shift+F` - глобальный поиск
- `Ctrl+Click` - открыть ссылку в новой вкладке

### Wiki-ссылки и пути

**Obsidian-style ссылки (предпочтительно):**
```markdown
[[0.6. Структура этого хранилища]]
[[4.1. Список подсистем#Основные системы]]
[[Agent-Template|Шаблон агента]]
```

**Markdown-style ссылки (для совместимости с GitHub):**
```markdown
[Структура](./content/0.%20Управление/0.6.%20Структура%20этого%20хранилища.md)
```

## Автоматизация и скрипты

### ops/classify_documents.py

**Назначение:** AI-классификация всех документов в `content/` по 6 осям.

**Использование:**
```bash
python3 ops/classify_documents.py

# Что делает:
# 1. Сканирует все .md файлы в content/
# 2. Анализирует содержимое через AI
# 3. Определяет type, audience, edit_mode, layer, scope, security
# 4. Обновляет таблицу в документе 0.6
# 5. Сохраняет защищенные (🟢) ячейки от перезаписи
```

**Внимание:** Скрипт НЕ изменяет ячейки с зеленым кругом 🟢 (ручные правки).

### ops/save_manual_edits.py

**Назначение:** Сохранение ручных правок в защищенный JSON-файл.

**Использование:**
```bash
# 1. Отредактируйте таблицу в документе 0.6 вручную
# 2. Запустите скрипт для сохранения правок
python3 ops/save_manual_edits.py

# Результат:
# - Создан/обновлен ops/manual_classifications.json
# - Все ячейки без 🟡 будут помечены как ручные (🟢)
```

**Формат JSON:**
```json
{
  "content/4. Системы/4.12. Единое хранилище знаний (Memory Bank).md": {
    "type": "doc",
    "layer": "architecture"
  }
}
```

### ops/validate_classifications.py

**Назначение:** Валидация всех значений классификации против схемы из документа 0.7.

**Использование:**
```bash
python3 ops/validate_classifications.py

# Проверяет:
# - Все значения type соответствуют допустимым
# - Все значения audience, edit_mode корректны
# - Все значения layer, scope, security валидны
# - Выводит список ошибок, если есть

# Exit code:
# 0 - все валидно
# 1 - найдены ошибки
```

**Пример вывода при ошибке:**
```
❌ Строка 12 (документ.md): невалидное значение 'custom-layer' для оси 'layer'.
   Допустимые значения: philosophy, methodology, ontology, program, ...
```

### ops/build_check_document.py

**Назначение:** Автоматическая сборка Проверочного документа (0.5) из разных источников.

**Использование:**
```bash
# Базовая сборка (без AI-анализа)
python3 ops/build_check_document.py

# С AI-анализом противоречий (требует OPENAI_API_KEY)
python3 ops/build_check_document.py --ai-analysis

# Через shell-скрипт (с AI-анализом)
./ops/build_check_document.sh
```

**Что собирает:**
1. Зачем, для кого, что мы делаем (из Манифеста, Концепции)
2. Проблемы, гипотезы, цели (из разделов 1.1-1.5)
3. Функционирование экосистемы (из 1.4)
4. Структура ИИ-платформы (из 4.2)
5. Список подсистем (из 4.1)
6. Схема взаимодействия (из 1.4)
7. Процесс разработки (из 0.8)
8. Классификация и теги (из 0.7)
9. Заключение о противоречиях (AI-анализ или ручное)

**AI-анализ противоречий:**
```bash
# Настройка API ключа
export OPENAI_API_KEY="your-key-here"

# Запуск с анализом
python3 ops/build_check_document.py --ai-analysis

# AI проверяет:
# - Соответствие целей и реализуемых систем
# - Согласованность процессов между разделами
# - Противоречия в классификации
# - Нестыковки в экономике
```

Подробнее: [ops/README.md](ops/README.md)

## Pull Request процесс

### Создание PR

**Через GitHub UI:**
1. Push вашу ветку в fork
2. Перейдите на https://github.com/ORIGINAL_ORG/ecosystem-development
3. Нажмите "Compare & pull request"
4. Заполните шаблон PR
5. Добавьте reviewers и labels

**Через GitHub CLI:**
```bash
gh pr create \
  --title "feat: добавлена система 4.13" \
  --body "$(cat <<EOF
## Описание

Добавлена новая подсистема 4.13. Система аналитики.

## Изменения

- Создан документ 4.13. Система аналитики.md
- Обновлен 4.1. Список подсистем.md
- Добавлена классификация в 0.6

## Чеклист

- [x] Добавлен frontmatter метаданные
- [x] Запущена классификация (ops/classify_documents.py)
- [x] Валидация пройдена (ops/validate_classifications.py)
- [x] Обновлен список подсистем
- [x] CI проверки проходят

## Связанные issues

Closes #42
EOF
)" \
  --base main \
  --label documentation
```

### Шаблон PR

```markdown
## Описание

Краткое описание того, что было изменено и почему.

## Тип изменения

- [ ] Новая функция/система
- [ ] Обновление документации
- [ ] Исправление ошибки
- [ ] Рефакторинг
- [ ] Обновление конфигурации

## Изменения

- Пункт 1
- Пункт 2
- Пункт 3

## Чеклист

- [ ] Код следует стандартам проекта
- [ ] Добавлены frontmatter метаданные (для новых документов)
- [ ] Запущены локальные проверки (classify, validate)
- [ ] Обновлена связанная документация
- [ ] CI проверки проходят
- [ ] Добавлены связи между документами (wiki-ссылки)

## Связанные issues

Closes #issue_number

## Скриншоты (если применимо)

## Дополнительный контекст
```

### Процесс ревью

**Что проверяют ревьюеры:**
1. ✅ Frontmatter метаданные корректны
2. ✅ Классификация соответствует осям из 0.7
3. ✅ Markdown форматирование соответствует стандартам
4. ✅ Связи между документами актуальны
5. ✅ Нет опечаток и грамматических ошибок
6. ✅ CI проверки проходят
7. ✅ Изменения согласуются с философией проекта

**Типы комментариев:**
- 🚫 **Blocking** - требует исправления перед merge
- 💡 **Suggestion** - рекомендация, не блокирует
- ❓ **Question** - вопрос для уточнения
- 👍 **Approval** - одобрение изменений

**Минимум для merge:**
- 2 approvals от команды
- Все CI проверки зеленые
- Все blocking комментарии resolved

## Стандарты качества

### CI/CD проверки

При каждом PR запускаются автоматические проверки:

**1. Frontmatter validation**
```yaml
# .github/workflows/content-ci.yml
- Проверка наличия frontmatter во всех .md
- Валидация обязательных полей
- Проверка значений против схемы
```

**2. Markdown linting**
```yaml
# .github/workflows/markdown-lint.yml
- Проверка форматирования markdown
- Правила из .markdownlint.json
```

**3. Naming conventions**
```yaml
# .github/workflows/naming-check.yml
- Проверка имен файлов
- Корректность структуры папок
```

**4. Document classification**
```yaml
# .github/workflows/classify-documents.yml
- Автоклассификация новых документов
- Обновление таблицы в 0.6
```

**5. Check document build**
```yaml
# .github/workflows/check-document-build.yml
- Сборка проверочного документа
- Проверка на противоречия (если настроен OPENAI_API_KEY)
```

### Ручные проверки перед commit

```bash
# 1. Проверить форматирование
# Используйте prettier, markdownlint или встроенный в редактор

# 2. Запустить классификацию
python3 ops/classify_documents.py

# 3. Валидировать метаданные
python3 ops/validate_classifications.py

# 4. Собрать проверочный документ (опционально)
python3 ops/build_check_document.py

# 5. Проверить что все работает
git status  # Нет лишних файлов?
git diff    # Изменения выглядят корректно?
```

### Качество документации

**Хороший документ:**
- ✅ Имеет четкую структуру (заголовки, разделы)
- ✅ Содержит frontmatter метаданные
- ✅ Краткое описание в начале (1-2 предложения)
- ✅ Ссылки на связанные документы
- ✅ Примеры, если применимо
- ✅ Актуальная информация
- ✅ Без опечаток и грамматических ошибок

**Плохой документ:**
- ❌ Нет структуры, "стена текста"
- ❌ Нет frontmatter метаданных
- ❌ Неясная цель документа
- ❌ Устаревшая информация
- ❌ Много опечаток
- ❌ Нет связей с другими документами

## Часто задаваемые вопросы

### Q: Как добавить новую систему в раздел 4?

```bash
# 1. Создайте новый документ
# content/4. Системы/4.13. Название системы.md

# 2. Добавьте frontmatter
---
type: doc
audience: mixed
edit_mode: manual
layer: service
scope: local-edge
security: internal
status: draft
---

# 4.13. Название системы

Краткое описание системы...

## Назначение
## Архитектура
## Интеграции
## Связанные документы

# 3. Обновите список подсистем
# content/4. Системы/4.1. Список подсистем.md

# 4. Запустите классификацию
python3 ops/classify_documents.py

# 5. Создайте PR
git checkout -b feature/add-system-13
git add content/4.*
git commit -m "feat: добавлена система 4.13. Название"
git push origin feature/add-system-13
gh pr create
```

### Q: Как защитить мою ручную правку от AI?

```bash
# 1. Отредактируйте таблицу классификации в документе 0.6
#    Замените 🟡 на нужное значение (без эмодзи)

# 2. Сохраните правку как защищенную
python3 ops/save_manual_edits.py

# 3. Теперь AI не перезапишет вашу правку (будет 🟢)
python3 ops/classify_documents.py
```

### Q: Что делать если CI проверки не проходят?

```bash
# 1. Посмотрите логи в GitHub Actions
#    Actions → Выберите failed workflow → Смотрите logs

# 2. Типичные проблемы:

# Frontmatter validation failed:
# - Проверьте что frontmatter присутствует
# - Проверьте валидность YAML синтаксиса
# - Убедитесь что значения соответствуют схеме

# Markdown linting failed:
# - Запустите markdownlint локально
# - Исправьте форматирование

# Naming convention failed:
# - Переименуйте файлы согласно правилам
# - Используйте пробелы, не дефисы

# 3. Исправьте проблемы, сделайте commit и push
git add .
git commit -m "fix: исправлены проблемы CI"
git push
```

### Q: Как работать с большими изменениями?

**Создайте draft PR:**
```bash
gh pr create --draft \
  --title "WIP: большое изменение X" \
  --body "Работа в процессе, не готово к ревью"

# Когда готово:
gh pr ready
```

**Или используйте feature-ветку долгое время:**
```bash
# Регулярно синхронизируйтесь с main
git checkout feature/big-change
git fetch upstream
git merge upstream/main
git push origin feature/big-change
```

### Q: Можно ли использовать AI для написания документов?

**Да, но с оговорками:**
- ✅ AI может помочь с черновиками
- ✅ AI может предложить структуру
- ✅ AI может проверить грамматику
- ❌ AI не должен принимать стратегические решения
- ❌ AI не должен изменять философию проекта
- ❌ Всегда проверяйте и редактируйте AI-генерированный контент

**Рекомендуемый workflow:**
1. Используйте AI для создания черновика
2. Тщательно проверьте и отредактируйте
3. Добавьте человеческий контекст и примеры
4. Убедитесь что документ соответствует философии проекта
5. Пометьте в PR что использовался AI (опционально)

### Q: Как обновить существующий документ?

```bash
# 1. Создайте ветку
git checkout -b docs/update-system-12

# 2. Отредактируйте документ
# Обновите содержимое, проверьте frontmatter

# 3. Если изменили классификацию, запустите скрипты
python3 ops/classify_documents.py
python3 ops/validate_classifications.py

# 4. Commit и PR
git add content/4.*
git commit -m "docs: обновлен документ 4.12. Memory Bank"
git push origin docs/update-system-12
gh pr create
```

### Q: Как добавить нового AI-агента?

```bash
# 1. Создайте описание агента
# content/agents/agent-name.md
# Используйте шаблон: content/templates/Agent-Template.md

# 2. Создайте манифест
# agents-core/agent-name/manifest.json

# 3. Добавьте индекс
# Обновите content/agents/00-Index.md

# 4. Классифицируйте
python3 ops/classify_documents.py

# 5. PR
git checkout -b feature/add-agent-name
git add content/agents/ agents-core/
git commit -m "feat: добавлен агент agent-name"
git push
gh pr create
```

## Контакты и поддержка

**Вопросы по участию:**
- GitHub Discussions: [обсуждения](https://github.com/ORG/ecosystem-development/discussions)
- GitHub Issues: [создать issue](https://github.com/ORG/ecosystem-development/issues)

**Команда:**
- Tech Lead: [контакт]
- Documentation: [контакт]
- Community: [контакт]

**Каналы:**
- Telegram: [группа]
- Discord: [сервер]

---

Спасибо за вклад в проект! 🚀

Если у вас есть вопросы или предложения по улучшению этого руководства, создайте issue или PR.
