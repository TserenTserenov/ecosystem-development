name: Build Check Document

on:
  # ОТКЛЮЧЕНО: автосборка без AI-анализа не имеет смысла
  # Запуск при пуше в основные ветки - ОТКЛЮЧЕН
  # push:
  #   branches:
  #     - main
  #     - feat/ci-and-ops
  #   paths:
  #     - 'content/**/*.md'
  #     - 'ops/build_check_document.py'

  # Только ручной запуск
  workflow_dispatch:

  # Еженедельный запуск - ОТКЛЮЧЕН (без AI-анализа бесполезен)
  # schedule:
  #   - cron: '0 9 * * 1'

jobs:
  build-check-document:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Build Check Document
        run: |
          python3 ops/build_check_document.py

      - name: Check for changes
        id: check_changes
        run: |
          if git diff --quiet content/0.\ Управление/0.5.\ Проверочный\ документ.md; then
            echo "changed=false" >> $GITHUB_OUTPUT
          else
            echo "changed=true" >> $GITHUB_OUTPUT
          fi

      - name: Commit changes
        if: steps.check_changes.outputs.changed == 'true'
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add "content/0. Управление/0.5. Проверочный документ.md"
          git commit -m "chore: auto-update check document

          🤖 Автоматически обновлен Проверочный документ (0.5)

          Generated with [Claude Code](https://claude.com/claude-code)"

      - name: Push changes
        if: steps.check_changes.outputs.changed == 'true'
        uses: ad-m/github-push-action@master
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          branch: ${{ github.ref }}

      - name: Create summary
        run: |
          echo "### 📊 Результаты сборки Проверочного документа" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          if [ "${{ steps.check_changes.outputs.changed }}" == "true" ]; then
            echo "✅ Документ обновлен и закоммичен" >> $GITHUB_STEP_SUMMARY
          else
            echo "ℹ️ Изменений не обнаружено" >> $GITHUB_STEP_SUMMARY
          fi
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "📍 Файл: \`content/0. Управление/0.5. Проверочный документ.md\`" >> $GITHUB_STEP_SUMMARY
          echo "⏰ Время сборки: $(date)" >> $GITHUB_STEP_SUMMARY
